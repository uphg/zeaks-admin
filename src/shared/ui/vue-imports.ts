// 全局Vue导入，用于TSX文件
export { createApp, defineComponent, ref, computed, watch, inject, provide, readonly } from 'vue'
export type { PropType, RenderFunction } from 'vue'
export type { ClassValue as ClassNameValue } from 'clsx'